// Оставил этот класс для примера проделанной работы, я его полностью перенес в класс Validator,
//как мне показалость так будет лучше соблюдаться OpenClosed

public class TeacherService {
// переместил этот код что бы соблюдать OpenClosed
//    public boolean validateTeacher(Teacher teacher) {
//        // Валидация учителя (например, проверка на заполненность полей)
//        return teacher.getName() != null && !teacher.getName().isEmpty() && teacher.getAge() > 0;
//    }
}

